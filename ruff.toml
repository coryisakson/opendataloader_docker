line-length = 100
target-version = "py310"

extend-exclude = [
  ".git",
  "node",
  "java",
  "docs",
  "content",
  "samples",
  "tests/benchmark",
]

[lint]
select = ["E", "F", "I", "W", "UP", "B"]

[format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"
